import 'package:flutter/material.dart';

class homeConsumer extends StatelessWidget {
  const homeConsumer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("home page for Consumer"),
      ),
    );
  }
}
